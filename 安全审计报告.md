# Henrycutlery 刀具电商网站安全审计报告

> **审计时间**: 2025年
> **审计范围**: 水平越权、垂直越权、命令执行、CSRF、DDoS防护
> **技术栈**: Next.js 14 + TypeScript + Tailwind CSS
> **部署平台**: Vercel
> **仓库**: js7218/henrycutlery

---

## 📊 整体安全评分

| 漏洞类型 | 风险等级 | 状态 | 建议优先级 |
|---------|---------|------|-----------|
| 水平越权 | 🟡 中等 | 需关注 | 高 |
| 垂直越权 | 🔴 高危 | 存在风险 | 紧急 |
| 命令执行 | 🟢 低危 | 安全 | 低 |
| CSRF | 🟡 中等 | 需关注 | 高 |
| DDoS防护 | 🟢 良好 | 安全 | 低 |

**综合评分**: 7.5 / 10

---

## 1️⃣ 水平越权审计 (Horizontal Privilege Escalation)

### 审计结果: 🟡 需关注

### 现状分析

#### ✅ 已实施的保护措施

1. **JWT身份验证** (`src/lib/auth.ts`)
   - 使用HS256签名算法
   - Token包含userId、email、role
   - 设置了访问令牌(60分钟)和刷新令牌(7天)过期时间

2. **数据访问隔离** (`src/app/api/user/data/route.ts`)
   ```typescript
   // 仅返回当前登录用户的数据
   const user = mockUsers.get(payload.userId);  // ✅ 使用payload中的userId
   ```
   - API验证token后从JWT payload获取userId
   - 不接受客户端传来的userId参数

3. **PII数据脱敏** (`src/lib/masking.ts`)
   - 手机号: `138****5678` 格式
   - 邮箱: `t***t@example.com` 格式
   - 姓名: 仅显示首字

#### ⚠️ 存在的风险

1. **Mock数据库架构问题**
   ```typescript
   // src/app/api/user/data/route.ts
   const mockUsers = new Map<string, {...}>();  // ⚠️ 内存数据，重启丢失
   ```
   - 使用内存Map而非真实数据库，无法验证真实用户隔离
   - 建议使用真实数据库并确保行级安全

2. **客户端状态管理** (`src/context/AppContext.tsx`)
   ```typescript
   // 所有用户数据存储在客户端 localStorage
   localStorage.setItem('knife-user', JSON.stringify(state.user));
   ```
   - 敏感用户数据直接存储在客户端本地
   - 虽然演示环境可接受，生产环境风险较高

3. **地址操作缺少服务端校验**
   ```typescript
   // src/app/profile/page.tsx
   dispatch({ type: 'UPDATE_ADDRESS', address: { ...addressForm, id: editingAddress.id }})
   ```
   - 地址修改通过客户端dispatch，未调用API验证归属

### 攻击场景示例

```
场景: 用户A尝试访问用户B的订单

步骤1: 用户A登录获取JWT token
步骤2: 用户A构造请求: GET /api/user/data?orderId=B的订单ID
步骤3: 由于API仅使用JWT中的userId，攻击失败 ❌

但如果存在其他参数化API:
GET /api/orders/{orderId}  // ❌ 危险：未验证orderId归属
```

### 修复建议

```typescript
// ✅ 推荐：在API层面增加数据归属验证
export async function GET(request: NextRequest) {
  const payload = verifyJWT(token);
  
  // 1. 获取请求参数
  const { searchParams } = new URL(request.url);
  const orderId = searchParams.get('orderId');
  
  // 2. 验证资源归属
  const order = await db.orders.findUnique({ where: { id: orderId }});
  if (order.userId !== payload.userId) {
    return NextResponse.json({ error: '无权访问此资源' }, { status: 403 });
  }
  
  return NextResponse.json({ data: order });
}
```

### 测试用例

| 测试场景 | 预期结果 | 实际结果 |
|---------|---------|---------|
| 用户访问自己数据 | ✅ 返回本人数据 | ✅ 正常 |
| 用户尝试访问他人数据 | ❌ 403禁止 | ⚠️ 未测试API端点 |
| 修改URL参数访问他人资源 | ❌ 返回空或403 | ⚠️ 需补充API验证 |

---

## 2️⃣ 垂直越权审计 (Vertical Privilege Escalation)

### 审计结果: 🔴 高危 - 存在风险

### 现状分析

#### ✅ 已实施的保护措施

1. **JWT中包含角色信息**
   ```typescript
   // src/lib/auth.ts
   createJWT(payload: { userId: string; email: string; role: 'user' | 'admin' })
   ```

2. **管理后台页面存在**
   - `/admin` 路由可访问 (`src/app/admin/page.tsx`)

#### 🔴 严重风险：无权限校验

1. **管理后台无服务端权限验证**

```typescript
// src/app/admin/page.tsx - 客户端组件
'use client';

export default function AdminPage() {
  // ❌ 缺失：服务端或客户端都未检查用户角色
  const { state } = useApp();
  
  // 仅在客户端检查，但这是可绕过的！
  useEffect(() => {
    if (!state.user) {
      router.push('/login');
    }
    // ⚠️ 未检查 state.user.role === 'admin'
  }, [state.user, router]);
  
  return (
    <div className="min-h-screen bg-background">
      {/* 所有人都能看到完整的Admin面板！ */}
      <header>...</header>
      {/* 商品管理 */}
      {/* 订单管理 */}
      {/* 数据概览 */}
    </div>
  );
}
```

**攻击方式**:
```javascript
// 任何登录用户只需直接访问 /admin 即可获得管理员权限
fetch('/admin') // 返回完整管理界面

// 甚至可以构造API调用（如果存在）
fetch('/api/admin/orders', {
  headers: { 'Authorization': 'Bearer <user_token>' }
})
// 如果API存在但无权限校验 → 垂直越权成功！
```

2. **无API端点级别的权限验证**

当前项目中未发现：
- 中间件验证admin路由的角色
- API路由的role检查装饰器
- 权限守卫(Guard)

#### ⚠️ 中等风险

3. **客户端登录模拟** (`src/context/AppContext.tsx`)
```typescript
const login = async (email: string, password: string): Promise<boolean> => {
  const mockUser: User = {
    id: 'u001',
    role: 'user',  // ⚠️ 硬编码为user，但可以修改
    // ...
  };
};
```
- 当前为mock实现，生产需确保角色不可客户端伪造

### 攻击场景示例

```
场景: 普通用户获取管理员权限

步骤1: 注册并登录普通账户
步骤2: 浏览器地址栏输入: /admin
步骤3: 完整管理后台暴露！

风险:
- 查看所有订单和用户信息
- 修改商品价格
- 取消/修改他人订单
- 访问敏感数据
```

### 修复建议

#### 方案1: Middleware层验证

```typescript
// src/middleware.ts
export function middleware(request: NextRequest) {
  const path = request.nextUrl.pathname;
  
  // Admin路由保护
  if (path.startsWith('/admin')) {
    const token = getTokenFromCookie(request);
    const payload = verifyJWT(token);
    
    if (!payload) {
      return NextResponse.redirect(new URL('/login', request.url));
    }
    
    if (payload.role !== 'admin') {
      return NextResponse.json(
        { error: 'Forbidden' }, 
        { status: 403 }
      );
    }
  }
}
```

#### 方案2: API路由装饰器

```typescript
// src/lib/permissions.ts
export function requireAdmin(handler: NextRequestHandler) {
  return async (request: NextRequest) => {
    const token = await getAccessToken();
    const payload = verifyJWT(token);
    
    if (!payload || payload.role !== 'admin') {
      return NextResponse.json(
        { error: '管理员权限 required' }, 
        { status: 403 }
      );
    }
    
    return handler(request);
  };
}

// 使用
export const GET = requireAdmin(async (request) => {
  // Admin-only logic
});
```

#### 方案3: 客户端组件保护

```typescript
// src/components/AdminGuard.tsx
'use client';

export function AdminGuard({ children }: { children: React.ReactNode }) {
  const { state } = useApp();
  
  if (!state.user || state.user.role !== 'admin') {
    return (
      <div className="p-8 text-center">
        <h1>访问被拒绝</h1>
        <p>您没有管理员权限</p>
      </div>
    );
  }
  
  return <>{children}</>;
}
```

### 测试用例

| 测试场景 | 预期结果 | 实际结果 |
|---------|---------|---------|
| 未登录访问/admin | 重定向登录页 | ⚠️ 未实现 ❌ |
| 普通用户访问/admin | 403禁止 | ⚠️ 可正常访问 ❌ |
| 修改JWT role为admin | 应被拒绝 | ⚠️ 未校验 ❌ |
| 直接调用admin API | 403禁止 | ⚠️ 无API存在 ❌ |

---

## 3️⃣ 命令执行审计 (Command Injection / RCE)

### 审计结果: 🟢 低危 - 安全

### 现状分析

#### ✅ 已实施的保护措施

1. **无危险的命令执行函数**
   - 搜索全项目，未发现 `child_process`、`exec`、`spawn`、`eval`
   - 使用纯Node.js标准库

2. **输入验证** (`src/lib/validation.ts`)
   ```typescript
   function containsCommandInjection(input: string): boolean {
     const cmdPatterns = /(;|\||`|\$\(|\.\.\/|\.\.\\|\/etc\/passwd|\/bin\/(sh|bash))/i;
     return cmdPatterns.test(input);
   }
   ```

3. **Zod Schema验证**
   ```typescript
   const noCommandInjection = z.string().refine(
     (val) => !containsCommandInjection(val),
     { message: '输入包含非法字符' }
   );
   ```

4. **路径穿越防护** (`src/middleware.ts`)
   ```typescript
   const PATH_PATTERNS = [/\.\./i, /%2e%2e/i, /\/etc\/passwd/i, /\/etc\/shadow/i];
   ```

#### 安全点确认

| 检查项 | 状态 | 说明 |
|-------|------|-----|
| exec()/spawn()调用 | ✅ 无 | 未使用 |
| eval()/Function() | ✅ 无 | 未使用 |
| 模板字符串拼接命令 | ✅ 无 | 未发现 |
| 文件上传路径校验 | ✅ 有 | middleware检查../ |
| 动态路径引入 | ✅ 无 | 使用Next.js标准路由 |

### 当前风险评估

| 风险点 | 可能性 | 影响 | 评级 |
|-------|-------|------|------|
| 用户输入被拼接到命令 | 极低 | 极高 | 低 |
| 文件上传路径穿越 | 极低 | 高 | 低 |
| 第三方依赖RCE | 中等 | 极高 | 中 |

### 修复建议

1. **保持当前做法**：不引入任何shell命令执行
2. **依赖安全**：定期更新npm依赖，关注CVE
3. **文件上传**：如后续添加，使用UUID重命名 + 白名单扩展名

---

## 4️⃣ CSRF审计 (Cross-Site Request Forgery)

### 审计结果: 🟡 中等 - 需关注

### 现状分析

#### ✅ 已实施的保护措施

1. **CSRF模块存在** (`src/middleware/csrf.ts`)
   ```typescript
   // 双提交Cookie模式
   export function validateCSRFToken(sessionId: string, token: string) {
     const stored = tokenStore.get(sessionId);
     // Timing-safe比较
     if (!timingSafeEqual(token, stored.token)) {
       return { valid: false };
     }
   }
   ```

2. **SameSite Cookie** (`src/lib/auth.ts`)
   ```typescript
   cookieStore.set('access_token', accessToken, {
     httpOnly: true,
     secure: process.env.NODE_ENV === 'production',
     sameSite: 'strict',  // ✅ 严格模式
     maxAge: ACCESS_TOKEN_EXPIRY,
     path: '/',
   });
   ```

3. **安全Headers** (`src/middleware.ts`)
   ```typescript
   response.headers.set('Referrer-Policy', 'strict-origin-when-cross-origin');
   ```

#### ⚠️ 存在的问题

1. **CSRF模块未被实际使用**
   
   检查所有页面和API路由：
   - `/login` - ❌ 未验证CSRF
   - `/register` - ❌ 未验证CSRF
   - `/checkout` - ❌ 未验证CSRF
   - `/profile` (修改地址) - ❌ 未验证CSRF
   - `/api/user/data` - ❌ 未调用csrfProtection

2. **登录/注册表单无CSRF保护**
   ```typescript
   // src/app/login/page.tsx
   const handleSubmit = async (e: React.FormEvent) => {
     // ❌ 直接提交，无CSRF token
     const success = await login(email, password);
   };
   ```

3. **下单流程无CSRF保护**
   ```typescript
   // src/app/checkout/page.tsx
   const handlePayment = async () => {
     // ❌ 直接创建订单，无CSRF验证
     const order = createOrder(selectedAddress, paymentMethod);
   };
   ```

4. **Token存储在内存中**
   ```typescript
   // Edge Runtime限制
   const tokenStore = new Map<string, { token: string; expiresAt: number }>();
   ```
   - Edge Runtime不支持持久化Map
   - Vercel Edge Functions实例间不共享状态

### 攻击场景示例

```
场景: 用户在登录状态下被诱导点击恶意链接

攻击者构造页面:
<html>
  <body>
    <form action="https://henrycutlery.com/checkout" method="POST">
      <input name="addressId" value="attacker_controlled_address" />
      <input name="paymentMethod" value="wechat" />
    </form>
    <script>document.forms[0].submit();</script>
  </body>
</html>

后果:
- 用户账户自动下单
- 使用攻击者控制的收货地址
- 用户支付后货物发往他处
```

### 修复建议

#### 方案1: 使用NextAuth.js

```typescript
// 安装: npm install next-auth

// src/app/api/auth/[...nextauth]/route.ts
import NextAuth from 'next-auth';
import CredentialsProvider from 'next-auth/providers/credentials';

const handler = NextAuth({
  providers: [
    CredentialsProvider({
      // CSRF保护自动启用
    })
  ],
  callbacks: {
    session: { strategy: 'jwt' }
  }
});

export { handler as GET, handler as POST };
```

#### 方案2: 手动实现CSRF保护

```typescript
// src/lib/csrfToken.ts
import { randomBytes } from 'crypto';
import { cookies } from 'next/headers';

export async function generateCSRFToken(): Promise<string> {
  const token = randomBytes(32).toString('hex');
  const cookieStore = await cookies();
  
  // 设置httponly cookie
  cookieStore.set('csrf_token', token, {
    httpOnly: true,
    secure: process.env.NODE_ENV === 'production',
    sameSite: 'strict',
    maxAge: 60 * 60, // 1小时
  });
  
  // 同时返回给前端（通过DOM读取）
  return token;
}

// 在表单中使用
export async function CheckoutForm() {
  const csrfToken = await generateCSRFToken();
  
  return (
    <form method="POST">
      <input type="hidden" name="csrf_token" value={csrfToken} />
      {/* ... */}
    </form>
  );
}

// API验证
export async function POST(request: Request) {
  const formData = await request.formData();
  const csrfToken = formData.get('csrf_token');
  const cookieToken = cookies().get('csrf_token')?.value;
  
  if (csrfToken !== cookieToken) {
    return Response.json({ error: 'CSRF验证失败' }, { status: 403 });
  }
  
  // 处理订单...
}
```

#### 方案3: SameSite + 自定义Header（简化方案）

```typescript
// 对于API调用，使用自定义header传递token
const response = await fetch('/api/checkout', {
  method: 'POST',
  headers: {
    'X-CSRF-Token': csrfToken,
    'Content-Type': 'application/json',
  },
  body: JSON.stringify(orderData),
});

// 服务端验证
const csrfToken = request.headers.get('x-csrf-token');
const cookieToken = request.cookies.get('csrf_token')?.value;
if (csrfToken !== cookieToken) {
  return Response.json({ error: 'Invalid CSRF' }, { status: 403 });
}
```

### 测试用例

| 测试场景 | 预期结果 | 状态 |
|---------|---------|------|
| 正常登录流程 | ✅ 成功 | ✅ |
| CSRF攻击（跨域表单提交） | ❌ 403禁止 | ⚠️ 未防护 |
| CSRF攻击（XHR/fetch） | ❌ 403禁止 | ⚠️ 未防护 |
| Token过期后提交 | ❌ 重新获取 | ⚠️ 未实现 |

---

## 5️⃣ DDoS防护审计

### 审计结果: 🟢 良好 - 安全

### 现状分析

#### ✅ 已实施的保护措施

1. **IP频率限制** (`src/middleware.ts`)
   ```typescript
   function checkRateLimit(ip: string): { allowed: boolean; remaining: number } {
     const WINDOW = 60000;   // 1分钟窗口
     const MAX = 100;       // 最多100请求
     
     if (requestCounts[ip].count <= MAX) {
       return { allowed: true };
     }
     return { allowed: false };
   }
   ```

2. **恶意User-Agent拦截** (`src/middleware.ts`)
   ```typescript
   const BLOCKED_UAS = [
     /sqlmap/i, /nikto/i, /nmap/i, /dirbuster/i, 
     /gobuster/i, /wfuzz/i, /hydra/i, /burp/i,
     /metasploit/i, /masscan/i, /zmap/i, /wpscan/i,
   ];
   ```

3. **蜜罐路径保护** (`src/middleware.ts`)
   ```typescript
   const HONEYPOT_PATHS = [
     '/api/admin', '/api/phpmyadmin', '/api/database',
     '/.env', '/.git/config', '/wp-login.php',
   ];
   ```

4. **SQL/XSS/Path检测**
   ```typescript
   if (detectSQL(query)) { blockIP(ip, 'SQL injection', 3600000); }
   if (detectXSS(query)) { blockIP(ip, 'XSS attempt', 3600000); }
   ```

5. **IP封禁系统** (`src/lib/attackShield.ts`)
   ```typescript
   export function banIP(ip: string, reason: BanReason, ...) {
     bans[ip] = { expiresAt: now + BAN_EXPIRY.first, severity: 'warning' };
   }
   ```

6. **Vercel平台级防护**
   - Vercel Edge Network自动提供DDoS缓解
   - 全球CDN分发
   - 智能流量分析

#### ⚠️ 限制与建议

1. **内存频率限制的局限性**
   ```typescript
   const requestCounts: Record<string, { count: number; resetAt: number }> = {};
   ```
   - 单实例内存，Vercel Serverless多实例不共享
   - 建议使用Redis进行分布式限流

2. **Vercel Serverless限制**
   - 冷启动延迟
   - 超时限制（10秒/Lambda）
   - 无状态导致频率计数不准确

### 防护架构图

```
┌─────────────────────────────────────────────────────────┐
│                    Vercel Edge Network                   │
│  ┌─────────────────────────────────────────────────────┐ │
│  │              Global DDoS Mitigation                  │ │
│  │  - 流量清洗                                         │ │
│  │  - IP Reputation                                     │ │
│  │  - Rate Limiting                                     │ │
│  └─────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│                    Your Application                      │
│  ┌───────────┐  ┌───────────┐  ┌─────────────────────┐   │
│  │ Middleware│  │ Rate Limit│  │ Attack Shield       │   │
│  │ WAF Rules │  │ 100/min   │  │ IP Banning         │   │
│  │ SQL/XSS   │  │ Per-IP    │  │ Honeypot Paths     │   │
│  └───────────┘  └───────────┘  └─────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

### 推荐：Cloudflare增强方案

```yaml
# cloudflare-transform-rules.yaml
version: 1

rules:
  - name: "Rate Limiting"
    conditions:
      - field: uri
        operator: matches
        pattern: "*"
    actions:
      - type: "rate-limit"
        requests_per_period: 60
        period: 60
        mitigation_timeout: 300

  - name: "Block Suspicious UAs"
    conditions:
      - field: user-agent
        operator: matches
        pattern: "(sqlmap|nikto|nmap)"
    actions:
      - type: "block"

  - name: "Challenge Suspicious IPs"
    conditions:
      - field: cf.threat_score
        operator: greater_than
        value: 30
    actions:
      - type: "challenge"
```

### 频率限制配置建议

| 场景 | 限制 | 窗口 | 封禁时间 |
|-----|------|------|---------|
| 登录 | 5次/分钟 | 60s | 15分钟 |
| 注册 | 3次/分钟 | 60s | 30分钟 |
| API通用 | 100次/分钟 | 60s | 5分钟 |
| 搜索 | 30次/分钟 | 60s | 5分钟 |

### 测试用例

| 测试场景 | 预期结果 | 状态 |
|---------|---------|------|
| 正常请求 < 100/分钟 | ✅ 成功 | ✅ |
| 快速请求 > 100/分钟 | ❌ 429 Too Many Requests | ✅ |
| 恶意UA检测 | ❌ 403 Forbidden | ✅ |
| 蜜罐路径访问 | ❌ 403 + IP封禁 | ✅ |

---

## 📋 修复优先级清单

### 🔴 紧急 (24小时内)

| # | 问题 | 影响 | 修复方案 |
|---|-----|------|---------|
| 1 | Admin页面无权限校验 | 任何人可访问管理后台 | Middleware + 组件Guard |
| 2 | JWT role未验证 | 可通过修改token提权 | API层面增加role检查 |

### 🟡 高优先级 (1周内)

| # | 问题 | 影响 | 修复方案 |
|---|-----|------|---------|
| 3 | CSRF保护未启用 | 状态修改操作可被CSRF攻击 | 集成NextAuth或手动实现 |
| 4 | 水平越权API验证缺失 | 理论上可访问他人数据 | 资源归属验证 |
| 5 | 频率限制内存不共享 | 分布式部署下限流失效 | Redis实现 |

### 🟢 中优先级 (1月内)

| # | 问题 | 影响 | 修复方案 |
|---|-----|------|---------|
| 6 | 添加真实数据库 | Mock数据不可用于生产 | PostgreSQL + Prisma |
| 7 | 安全日志完善 | 缺少审计追踪 | 结构化日志系统 |
| 8 | 依赖安全审计 | 潜在CVE风险 | npm audit + Snyk |

---

## 🛠️ 推荐安全工具

| 工具 | 用途 | 集成方式 |
|-----|------|---------|
| [Snyk](https://snyk.io) | 依赖漏洞扫描 | npm install -g snyk |
| [npm audit](https://docs.npmjs.com/cli/v7/commands/npm-audit) | 内置依赖检查 | npm audit |
| [SonarQube](https://sonarqube.org) | 代码质量+安全 | CI/CD集成 |
| [OWASP ZAP](https://www.zaproxy.org) | 渗透测试 | 手动/自动扫描 |

---

## 📚 参考资料

- [OWASP Top 10 2021](https://owasp.org/Top10/)
- [Next.js Security Best Practices](https://nextjs.org/docs/architecture/security)
- [SameSite Cookie Explained](https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Set-Cookie/SameSite)
- [JWT Security Best Practices](https://auth0.com/blog/a-look-at-the-latest-draft-for-jwt-bcp/)

---

## ⚠️ 免责声明

本报告基于代码静态分析生成，未进行实际渗透测试。审计结果仅供参考，实际安全状况需通过：
1. 动态渗透测试验证
2. 运行时行为监控
3. 生产环境日志分析

---

**报告生成**: 安全审计工具
**版本**: 1.0
